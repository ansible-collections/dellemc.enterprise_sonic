---
- name: Preparations for test
  ansible.builtin.include_tasks: preparation_tests.yaml

- name: Test started ..."
  ansible.builtin.include_tasks: tasks_template.yml
  loop: "{{ sonic_evpn_esi_multihome_tests }}"
