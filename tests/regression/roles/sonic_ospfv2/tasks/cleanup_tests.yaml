---
- name: Delete OSPFv2 configurations
  sonic_ospfv2:
    config: []
    state: deleted
  failed_when: false

- name: Delete test VRFs
  sonic_vrfs:
    config: "{{ preparations_tests.vrfs }}"
    state: deleted
  failed_when: false
