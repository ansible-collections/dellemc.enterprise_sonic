---
- name: Deletes old radius server configurations
  sonic_radius_server:
    config: {}
    state: deleted
  failed_when: false

- name: Create test VRF
  sonic_vrfs:
    config:
      - name: VrfAnsibleTest
