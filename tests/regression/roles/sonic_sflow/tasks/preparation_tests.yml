- name: "initialize sflow test environment"
  vars:
    ansible_connection: network_cli
  sonic_config:
    commands: "{{ preparations_tests.init }}"
    
- name: "prep delete all sflow config"
  vars:
    ansible_connection: httpapi
  sonic_sflow:
    config: {}
    state: deleted