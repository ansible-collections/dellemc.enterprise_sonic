---
- name: Deletes old SNMPs
  sonic_snmp:
    config: {}
    state: deleted

- name: Delete old VRFs
  sonic_vrfs:
    config:
      - name: "{{ vrf1 }}"
      - name: "{{ vrf2 }}"
    state: deleted
