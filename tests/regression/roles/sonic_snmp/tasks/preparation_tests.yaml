---
- name: "Delete old snmps"
  dellemc.enterprise_sonic.sonic_snmp:
    config: {}
    state: deleted

- name: Initialize snmp
  vars:
    ansible_connection: network_cli
  dellemc.enterprise_sonic.sonic_config:
    commands: "{{ preparation_tests.init_group }}"
