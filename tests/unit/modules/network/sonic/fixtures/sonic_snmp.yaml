---
merged_01:
  module_args:
    config:
      - user:
        - name: user2
          auth:
            auth_type: sha
            key: shakey5
          priv:
            priv_type: aes
            key: aes-128
  existing_snmp_config:
    - path: '/data/openconfig-sonic-snmp:sonic-snmp'
      response:
        code: 200
  expected_config_requests:
    - path: '/data/openconfig-sonic-snmp:sonic-snmp'
      method: 'patch'
      data:
        openconfig-sonic-snmp:sonic-snmp:
          snmp-server:
            user:
              - name: user2
                auth:
                  config:
                    auth-type: 'sha'
                    auth-key: 'shakey5'
                priv:
                  config:
                    priv-type: 'aes'
                    priv-key: 'aes-128'

merged_02:
  module_args:
    config:
      - group:
        - name: group-floor2
          access:
            security_model: v3
            security_level: auth-priv
            read_view: r_view
            write_view: w_view
            notify_view: n_view
      - community:
        - group: group-lab
          name: comm1
  existing_snmp_config:
    - path: '/data/openconfig-sonic-snmp:sonic-snmp'
      response:
        code: 200
  expected_config_requests:
    - path: '/data/openconfig-sonic-snmp:sonic-snmp'
      method: 'patch'
      data:
        openconfig-sonic-snmp:sonic-snmp:
          snmp-server:
            group:
              - name: group-floor2
                access:
                  config:
                    security-model: 'v3'
                    security-level: 'auth-priv'
                    read-view: 'r_view'
                    write-view: 'w_view'
                    notify-view: 'n_view'
            community:
              - name: comm1
                config:
                  group: 'group-lab'
                  name: 'comm1'